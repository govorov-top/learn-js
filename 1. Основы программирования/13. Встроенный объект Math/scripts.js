// Встроенный объект Math https://clck.ru/KxJMP

// Рандомное число от 0 до 1 (1 никогда не будет)
let x = Math.random() * 10;
console.log(x);

// Округление в меньшую сторону
console.log(Math.floor(x));

// Округление в большую сторону
console.log(Math.ceil(x));

// Округление по математике, в ту сторону в которую логично
console.log(Math.round(x));