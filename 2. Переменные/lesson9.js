// Переменные - var(Устарел),const,let
var a = 'Name';
const b = 5+5; // В будущем не меняется, но если это тип объект можно менять свойства
let c = 7+7;

console.log(a,b,c);

// Правописание - camelCase, _ $ [A-Z] !^[0-9]
let firstName, lastName, forWork = '';
